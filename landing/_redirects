# Cloudflare Worker static-asset redirects for sentinelprotocol.co.uk
# Format: <from> <to> <status>.  See landing/README.md for the rationale.

# Company root -> the Digr product page (interim; becomes a real company
# page when a second product ships). 302 (NOT 301) so it can be changed later.
/            /digr                                          302

# Pro buy link -> the Gumroad product page. No discount code is baked in:
# pricing (free -> £19 -> £29) is driven by Gumroad's own scheduled windows,
# so this one link serves every launch phase with no redeploy.
/digr/pro    https://sentinelprotocol.gumroad.com/l/digr    302
